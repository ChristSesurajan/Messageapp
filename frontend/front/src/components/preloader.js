// Preloader.js
import React from 'react';
import './preloader.css'; // Import CSS for styling the preloader

const Preloader = () => (
  <div className="preloader">
    <div className="spinner"></div>
  </div>
);

export default Preloader;
